'use strict';

module.exports = {
    upgrade: true,
    // Nothing is pinned back at the moment. Add package names here if an upgrade
    // needs source changes that are not ready yet, so `npm run update` stays safe
    // to run unattended.
    reject: []
};
